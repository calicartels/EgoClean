huggingface_hub>=0.20
python-dotenv>=1.0
opencv-python-headless>=4.8
numpy>=1.24
# Choice: torch listed but likely pre-installed on Vast.ai PyTorch image.
# Alternative was pinning a specific CUDA build — but the Vast.ai image
# handles CUDA/torch compatibility already. Skip if already installed.
torch>=2.1
# Choice: transformers>=4.45 — Qwen2.5-VL support landed in 4.45.
# Alternative was installing from git main, but 4.45+ is stable on PyPI.
transformers>=4.45
# Choice: qwen-vl-utils for process_vision_info image handling.
# Alternative was manual image preprocessing — fragile and undocumented.
qwen-vl-utils>=0.0.8
# Choice: accelerate for device_map="cuda" in from_pretrained.
# Without it, device_map fails silently or errors.
accelerate>=0.26